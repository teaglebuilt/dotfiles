name: Tests
on:
  push:
  pull_request:

jobs:

  tests:
    name: Run full installation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Run install
        run : ./setup.sh